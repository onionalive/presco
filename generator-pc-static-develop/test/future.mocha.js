var helpers = require('yeoman-test');
var assert = require('yeoman-assert');
var path = require('path');

describe('future things to add (if we want)', function() {
	it('options for other template engines');
	it('options for other frameworks');
	it('example contentful files');
	it('options for other npm packages we usually use');
});
