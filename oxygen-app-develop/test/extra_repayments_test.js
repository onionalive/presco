import React, { View, Text, StyleSheet } from 'react-native';
import { shallow } from 'enzyme';
import { expect } from 'chai';
// import ExtraRepaymentsPathway from '../src/components/extrarepaymentspathway/ExtraRepaymentsPathway';
import ExtraRepayments from '../src/helpers/extraRepayments';

describe('ExtraRepayments helper module', () => {
	it('should return the correct values');
});

// describe('<ExtraRepaymentsPathway />', () => {
//   it('should render one View', () => {
//     const wrapper = shallow(<ExtraRepaymentsPathway />);
//     expect(wrapper.find(View)).to.have.length(1);
//   });
// });
