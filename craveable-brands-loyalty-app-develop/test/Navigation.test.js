import { expect, assert } from 'chai';
import { initTest } from '../app/common/Tranxactor';
import chalk from 'chalk';

/**
 * Navigation and External Access
 */
describe(chalk.yellow('Navigation functionality'), () => {
	describe(chalk.magenta('NAV01: Allow deep linking'), () => {
		describe('Allow deep linking to key pages within the app and website.', () => {

		});
	});

	describe(chalk.magenta('NAV04: Trigger app notifications'), () => {
		describe('Allow Marketing Cloud to trigger app notifications.', () => {

		});
	});

	describe(chalk.magenta('NAV05: Allow contextual messages to be triggered and displayed'), () => {
		describe('Allow contextual messages to be triggered via Marketing Cloud. This would be a ‘promo’ area of the app that would show up when the message is triggered. (Note: We’ll reprioritise this when we have a use case and the ability for the message to be triggered from Marketing Cloud.)', () => {

		});
	});

	describe(chalk.magenta('NAV06: View store details'), () => {
		describe('Provide a way for users to navigate to online ordering from the app.', () => {

		});
	});
});
