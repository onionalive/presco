import { expect, assert } from 'chai';
import { initTest } from '../app/common/Tranxactor';
import chalk from 'chalk';

/**
 * Feedback and Support
 */
describe(chalk.yellow('Tracking functionality'), () => {
	describe(chalk.magenta('FDB01: Display an onboarding message'), () => {
		describe('Display an onboarding message when the app is first launched.', () => {

		});
	});

	describe(chalk.magenta('FDB02: Replay onboarding message'), () => {
		describe('Ensure onboarding message is able to be replayed.', () => {

		});
	});

	describe(chalk.magenta('FDB03: Allow feedback to be sent via a native form'), () => {
		describe('Send feedback within the app / website without being taken to an external (or iframed) form.', () => {

		});
	});

	describe(chalk.magenta('FDB04: Include a "help" section'), () => {
		describe('Include a help/FAQs section.', () => {

		});
	});
});
