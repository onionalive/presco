import { expect, assert } from 'chai';
import { initTest } from '../app/common/Tranxactor';
import chalk from 'chalk';

describe(chalk.yellow('Products functionality'), () => {
	describe(chalk.magenta('PRD01: Retrieve product data dynamically'), () => {
		describe('Retrieve product data from an API.', () => {

		});
	});

	describe(chalk.magenta('PRD02: Organise products by category'), () => {
		describe('Organise products by category.', () => {

		});
	});

	describe(chalk.magenta('PRD03: Display all products'), () => {
		describe('Display all products for given brand.', () => {

		});
	});

	describe(chalk.magenta('PRD04: Display product prices'), () => {
		describe('Display pricing for products according to host store pricing zone. (not currently supported by API)', () => {

		});
	});
});
