import { expect, assert } from 'chai';
import { initTest } from '../app/common/Tranxactor';
import chalk from 'chalk';

describe(chalk.yellow('Card functionality'), () => {
	describe(chalk.magenta('CRD01: Scan card to app'), () => {
		describe('Use mobile phone camera to scan the barcode or QR code on a physical rewards card.', () => {

		});
	});

	describe(chalk.magenta('CRD02: Merge cards'), () => {
		describe('Allow cards to be merged in the app.', () => {

		});
	});

	describe(chalk.magenta('CRD05: Show card when app is landscape'), () => {
		describe('Display loyalty card when mobile device is in landscape orientation.', () => {

		});
	});

	describe(chalk.magenta('CRD08: Increase screen brightness on card display'), () => {
		describe('Temporarily increase screen brightness when card is displayed.', () => {

		});
	});

	describe(chalk.magenta('CRD09: Delete cards'), () => {
		describe('Delete cards.', () => {
		});
	});
});
