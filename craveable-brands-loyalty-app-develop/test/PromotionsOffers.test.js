import { expect, assert } from 'chai';
import { initTest } from '../app/common/Tranxactor';
import chalk from 'chalk';

/**
 * Promotions and Offers
 */
describe(chalk.yellow('Promotions and Offers functionality'), () => {
	describe(chalk.magenta('PRM01: List unused offers'), () => {
		describe('List all unused offers.', () => {

		});
	});

	describe(chalk.magenta('PRM02: List used offers'), () => {
		describe('List all used offers.', () => {

		});
	});

	describe(chalk.magenta('PRM03: List expired offers'), () => {
		describe('List all expired offers.', () => {

		});
	});

	describe(chalk.magenta('PRM04: Display offer detail'), () => {
		describe('Display details for each individual promotion.', () => {

		});
	});

	describe(chalk.magenta('PRM05: Display custom offer image'), () => {
		describe('Display a custom image for each offer. (Note: Requires work from Tranxactor and on the form used to create offers.)', () => {

		});
	});

	describe(chalk.magenta('PRM06: Display promotions'), () => {
		describe('Display current promotions.', () => {

		});
	});

	describe(chalk.magenta('PRM07: Display promotion detail'), () => {
		describe('Display details for each individual promotion.', () => {

		});
	});
});
