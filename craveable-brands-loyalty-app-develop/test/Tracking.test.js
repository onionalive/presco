import { expect, assert } from 'chai';
import { initTest } from '../app/common/Tranxactor';
import chalk from 'chalk';

/**
 * Tracking
 */
describe(chalk.yellow('Tracking functionality'), () => {
	describe(chalk.magenta('TRC01: Track platform interactions'), () => {
		describe('Track interactions in app and website using Firebase.', () => {
		});
	});

	describe(chalk.magenta('TRC02: Integrate Marketing Cloud SDK'), () => {
		describe('Include Marketing Cloud SDK to enable push notifications.', () => {

		});
	});

	describe(chalk.magenta('TRC03: Bluedot SDK'), () => {
		describe('Include Bluedot SDK for triggered geo services messaging via marketing cloud.', () => {

		});
	});
});
