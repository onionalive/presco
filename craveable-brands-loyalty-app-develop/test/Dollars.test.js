import { expect, assert } from 'chai';
import { initTest } from '../app/common/Tranxactor';
import chalk from 'chalk';

/**
 * Dollars
 */
describe(chalk.yellow('Dollars functionality'), () => {
	describe(chalk.magenta('PNT01: Display points'), () => {
		describe('Retrieve product data from an API.', () => {

		});
	});

	describe(chalk.magenta('PNT02: Display loyalty tier'), () => {
		describe('Organise products by category.', () => {

		});
	});

	describe(chalk.magenta('PNT03: Loyalty tiering info'), () => {
		describe('Describe how a user could progress to the next tier / drop to a lower tier.', () => {

		});
	});

	describe(chalk.magenta('PNT04: Display earn/burn rates'), () => {
		describe('Display earn/burn rates for current loyalty tier.', () => {

		});
	});
});
